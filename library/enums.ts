export enum DataType {
    VEGA = "vega",
    IMAGE = "image",
    EMPTY = "empty"
}

export enum ViewType {
    SINGLE = "single",
    DOUBLE = "double",
    TRIPLE = "triple",
    QUADRUPLE = "quadruple",
    CUSTOM = "custom"
}

export enum ViewId {
    FIRST = 0,
    SECOND = 1,
    THIRD = 2,
    FOURTH = 3
}