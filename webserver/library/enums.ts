export enum DataType {
    VEGA = "vega",
    IMAGE = "image",
    EMPTY = "empty"
}

export enum ViewType {
    SINGLE = "single",
    DOUBLE = "double",
    TRIPLE = "triple",
    QUADRUPLE = "quadruple",
    CUSTOM = "custom"
}